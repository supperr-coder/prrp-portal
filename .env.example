# Copy to .env and fill in values — never commit .env

# AWS / S3 (Maestro)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-southeast-1
S3_BUCKET_NAME=

# LLM (Anthropic)
ANTHROPIC_API_KEY=

# Backend API (when added)
# BACKEND_URL=http://backend:8000
